# @plexo/core

The core library of Plexo, providing essential utilities and foundational components for building reusable UI elements. This package includes core functionalities and abstractions needed to support `@plexo/components` and `@plexo/themes`.
